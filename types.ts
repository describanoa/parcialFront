export type apiPhoneData = {
    is_valid: boolean,
    country: string
}

export type apiCountryData = {
    capital: string,
    name: string
}

export type apiCapitalData = {
    capital: string,
    name: string,
    temp: number
}